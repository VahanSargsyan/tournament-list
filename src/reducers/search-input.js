// import { ADD_FAVORITE } from "../constants/action-types";
// import { getAllFavorites, addFavorite } from "../storage";

// const initialState = getAllFavorites();

// export default function favorites(state = initialState, action) {
//   switch (action.type) {
//     case ADD_FAVORITE:
//       addFavorite(action.payload);
//       return [...state, action.payload];
//     default:
//       return state;
//   }
// }
